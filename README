Description
-----------

Implements additional functions related to KineoWorks:
  - flatness based steering method for cart-like mobile robot,
  - flatness based steering method for car-like mobile robot,
  - Reeds and Shepp steering method for cart-like mobile robot,
  - derives CkppDeviceComponent into CcarlikeDevice in order to implement a car-like robot>


Unit Testing
------------

Unit tests are provided to test the flatness based steering methods.
To run these tests, compile the package, go to unitTesting (in build directory)
and type
  make license
in order to validate executable using KineoWorks.

Then you can run each test executable in the corresponding sub-directory:
  cd bintestxxx
  ./testxxx
